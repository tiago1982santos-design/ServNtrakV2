## Packages
date-fns | Essential for calendar logic and date formatting
lucide-react | Beautiful icons for the UI (already in base, but ensuring availability)
react-day-picker | For the calendar view component

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Authentication uses Replit Auth blueprint (via useAuth hook)
Mobile-first design approach is critical
